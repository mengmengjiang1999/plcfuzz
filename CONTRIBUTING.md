# 参与 PLCFuzz

感谢为 PLCFuzz 的学术软件质量研究做贡献。提交代码、测试用例或实验记录前，请确认工作对象和环境均已获得明确授权，并且实验只在隔离仿真环境或专用实验台中运行。

## 项目边界

- 不提交面向生产 PLC、现场设备或在役工业系统的操作步骤。
- 不把实验性启发式结果描述为已经确认的确定缺陷。
- 不把本项目描述为认证工具、生产运行时或功能安全评估工具。
- 新增样例应为自有、明确许可或可再分发的测试程序，并记录来源。

## 对外文字

维护中的 README、指南、日志和注释优先使用以下表达：

| 要表达的概念 | 推荐表述 |
| --- | --- |
| 项目活动 | 学术鲁棒性测试、软件质量实验、自动化输入生成 |
| 工具保存的输入 | 候选样本、异常行为样本、非正常终止样本 |
| 启发式观测 | 候选问题、异常输出变化、需要人工复核的行为 |
| 实验环境 | 经授权的隔离仿真环境、专用实验台 |

描述结论时应说明证据层级。例如，输出持续变化只能称为候选异常行为，不能直接等同于并发数据竞争。尽量在出现专业缩写的同一段说明其软件测试用途，避免让脱离上下文的短句改变项目含义。

## 必须保留的外部名称

AFL++ 的产品名、ABI 符号、环境变量和目录约定必须保持原样，否则会破坏构建或实验复现。公开论文标题也应准确引用。这些名称出现时，应在邻近文字中标明它们属于第三方接口、引用材料或历史记录。

`docs/archive/` 保存研究过程材料，不作为当前操作指南；`docs/research/` 中的文献标题属于引用内容。不要为了统一用语而改写引用标题或改变历史结论，但应保留文件顶部的上下文说明。

## 提交前检查

```sh
./scripts/check_project_wording.sh
./scripts/check_source_layout.sh
./scripts/test_unit.sh
./scripts/validate_testcases.sh
```

代码变更还应说明使用的仓库 commit、MatIEC gitlink、测试命令和实验配置。可复现要求见 [REPRODUCIBILITY.md](REPRODUCIBILITY.md)。
