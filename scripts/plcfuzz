#!/usr/bin/env bash
set -euo pipefail

repo_root=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)
echo "Deprecated compatibility command: use ./scripts/plc-lab instead." >&2
exec "$repo_root/scripts/plc-lab" "$@"
